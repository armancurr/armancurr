import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Student - Developer",
  description:
    "Explore the portfolio of Arman Kar, a passionate full stack developer and AI enthusiast. Discover innovative web projects, real-time applications, and insightful tech content. Connect on GitHub, LinkedIn, and Twitter to follow the journey in modern web development and artificial intelligence.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
